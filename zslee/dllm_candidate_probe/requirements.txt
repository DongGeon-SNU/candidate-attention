# Install only inside zslee/dllm_candidate_probe/.venv on the shared-GPU volume.
# The CUDA-compatible torch wheel is selected and pinned by scripts/setup.sh.
# Fast-dLLM v1 itself pins this version; keeping the project aligned prevents
# setup from silently installing then downgrading Transformers.
transformers==4.49.0
huggingface_hub>=0.30
safetensors>=0.5
PyYAML>=6.0
numpy>=1.26
matplotlib>=3.9
# Public benchmark loading and compact Parquet artifact writing for the
# top-1-dynamics audit. No credentials are required for the supported public
# datasets; loaders fail closed with a provenance record if access is blocked.
datasets>=3.0
pyarrow>=15.0
